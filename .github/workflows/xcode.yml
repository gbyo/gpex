name: Xcode

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  test:
    name: Build and test GpEx
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show Xcode version
        run: xcodebuild -version

      - name: Build and test
        run: |
          xcodebuild \
            -project GpEx.xcodeproj \
            -scheme GpEx \
            -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=latest' \
            test
